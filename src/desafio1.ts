// Como podemos rodar isso em um arquivo .ts sem causar erros? 

interface employee {
    name : String,
    code : Number,
    role : String
};

let employee1:employee = {
    name:'Eduardo',
    code: 555,
    role:'manager'
};